<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="shortcut icon" href="<%= image_path("favicon.ico") %>" type="image/x-icon"></link>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title><%= yield(:title).presence || "Mnny" %></title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= csrf_meta_tags %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', '<%= ENV["GOOGLE_ANALYTICS_ID"] %>', 'auto');
      ga('send', 'pageview');
    </script>
  </head>
  <body>
    <%= javascript_include_tag 'application' %>

    <div class="container root-container <%= controller_name %> <%= action_name %>">
      <% flash.each do |key, value| %>
        <div class="flash alert alert-<%= key %>"><%= value %></div>
      <% end %>

      <%= render("shared/navbar") %>

      <%= yield %>
    </div>
  </body>
</html>
